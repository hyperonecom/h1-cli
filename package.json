{
  "name": "root",
  "private": true,
  "scripts": {
    "release:rc": "lerna run build && lerna publish --canary",
    "release": "NODE_ENV=production lerna run build && lerna publish",
    "watch": "lerna run --parallel watch",
    "build": "lerna run build",
    "lint": "eslint .",
    "fmt": "eslint . --fix",
    "install": "lerna bootstrap",
    "install:ci": "lerna bootstrap",
    "clean": "lerna clean"
  },
  "workspaces": ["extensions/*", "packages/*"],
  "devDependencies": {
    "@hyperone/eslint-config": "^2.0.1",
    "eslint": "^7.4.0",
    "eslint-plugin-jasmine": "^4.1.1",
    "lerna": "^3.22.1"
  }
}
