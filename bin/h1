#!/usr/bin/env node
'use strict';

const { startCli } = require('./../');

const main = async () => startCli(process.argv.slice(2), {
    openapiUrl: 'https://api.hyperone.com/v2/openapi.json',
    device: 'node',
});

main().catch(console.log);
