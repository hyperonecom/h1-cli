#!/usr/bin/env node
'use strict';

const { startCli } = require('./../');

const main = async () => startCli(process.argv, {
    openapiUrl: 'https://api.rootbox.com/v2/openapi.json',
    device: 'node',
});

main().catch(console.log);
